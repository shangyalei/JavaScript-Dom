dom库总结：

1.获取元素

--浏览器自带获取元素的方法
  id
  className
  tagName
  querySelector
  querySelectorAll
--封装

  1.获取所有的哥哥元素节点

  2.获取所有的弟弟元素节点

  3.获取上一个哥哥元素节点

  4.获取下一个弟弟元素节点

  5.获取上一个哥哥和下一个弟弟元素节点

  6.获取所有的兄弟节点(不包含它自己)

  7.获取所有的子节点

  8.获取所有的子节点或则获取所有的指定标签名的子元素

  9.获取第一个子节点

  10.获取最后一个子节点

  11.判断是不是第一个子节点

  12.判断是不是最后一个子节点

  13.通过类名获取元素


2.操作CSS

  1.添加类名

  2.删除类名

  3.判断是否有这个类名

  4.获取样式

  5.设置样式

  6.批量设置样式

  7.综合获取、设置、批量设置

3.动态操作DOM

  1.插入容器的最后面

  2.插入容器的最前面

  3.插入指定元素的前面

  4.插入指定元素的后面


4.工具方法

  1.类数组转化为数组

  2.json字符串转化为json对象




